JScriptDir <- '.'
JSecureDir <- '.'
JInsecureDir <- '.'
JDataDir <- file.path(JInsecureDir, '/data')
JClassDir <- file.path(JInsecureDir, '.')
JCacheDir <- '.cache'

# Host address of web app with sample data, photos, videos etc
MORPHO_HOST <- '130.56.244.191'

source(file.path(JScriptDir, 'util.r'))
